// IIFE -> Immediately invoked function expression
// funções que são imediatamente invocadas

(function() {
    const nome = 'Luiz';
})();

const nome = 'Qualquer coisa'; // esta varialvel não tem nada a ver com a outra